// These should maybe be classes eventually but for now they're just to make the doc comments a bit more intuitive.
/** @typedef {number} TileIndex A number between 0 and 37 representing a tile. 0, 10, and 20 are red fives, and 30 is the back of a tile. */
/** @typedef {{value:number, tiles:number[]}} UkeireObject */
/** @typedef {number[]} TileCounts An array of length 38 containing the number of each tile in the section. */